#include <stdio.h>
#include <vector>
#include <set>
#include <map>
#include <unordered_map>
#include <deque>
#include <list>



int main()
{
    int a, b, c;
    scanf("%d %d %d", &a, &b, &c);
    printf("%d%d%d\n", a, b, c);
    return 0;
}