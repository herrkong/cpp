#include <stdio.h>
#include <vector>
#include <set>
#include <map>
#include <unordered_map>
#include <deque>
#include <list>

#define M y *y + 3 * y

int main()
{
    auto s = 3 * M + 4 * M + y * M;
    return 0;
}